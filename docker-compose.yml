version: '3.8'

services:
  web:
    build: .
    container_name: farmer_voice_ai_web
    restart: always
    ports:
      - "80:7860"
    env_file:
      - backend/.env
    volumes:
      - ./backend/db.sqlite3:/home/user/app/backend/db.sqlite3
      - ./backend/media:/home/user/app/backend/media
